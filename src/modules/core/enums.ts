export enum LinkTypes {
  InstagramLink = 0
}

export enum ModuleTypes {
  InstagramLinks = 1,
  PoltavaNews,
  Currency,
  SlackApp,
  Weather
}

export enum PostStrategies {
  AsSoonAsPossible = 1,
  RandomSingle = 2
}
